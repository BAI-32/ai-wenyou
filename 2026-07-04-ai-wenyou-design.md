# AI 文游微信小程序 · 完整设计文档（Spec v2.3）

> **日期**: 2026-07-04 (v2.3: 加入多存档系统 + mimo endpoint 确认)
> **状态**: 待用户最终审查 → 通过后转 writing-plans
> **范围**: 完整小程序，覆盖全部 10 痛点 + 用户压缩需求
> **位置**: `C:\Users\35017\Desktop\1111\xm\`
> **取代**: v1（MVP-only，保留参考）

---

## 0. 关键决策（已与用户确认）

| # | 决策点 | 选择 | 理由 |
|---|--------|------|------|
| 1 | 整体范围 | **可扩展优先**：架构支持多世界(`game_id`)，先精做一个世界 | 解决痛点10 |
| 2 | demo 世界 | **修仙·凡人修仙传人界**（天南越国七派区域起步） | 设定文档无剧情纯背景，契合"无固定剧情" |
| 3 | 受众/规模 | 多用户(微信 openid)，小规模(吃免费额度) | 小程序+云开发 |
| 4 | LLM | OpenAI 兼容，BYOK；**用户自配+切换模型是核心功能**；**mimo 作默认预填** | 主力 mimo(`endpoint=https://api.xiaomimimo.com/v1`, model=`mimo-v2.5-pro`, `/chat/completions`)+多中转站均兼容 |
| 5 | 流式 | **MVP 即流式**（云函数 SSE） | 用户要求 |
| 6 | 数值 | **叙事优先，无硬数值** | 用户不喜欢数值束缚；境界等定性 tier |
| 7 | 语言/模态 | 中文 / 纯文字 | 聚焦核心 |
| 8 | 内容安全 | `msgSecCheck` 最小过滤 | 微信平台要求 |
| 9 | 输出格式 | `REPLY/MEMORY/STATE/DIRECTOR` 四段单次调用 | 兼顾叙事+记忆+状态+线索，省调用 |
| 10 | 压缩 | **采纳 caveman-compress 规则**，移植为云函数压缩 prompt | Python 原版无法在云函数跑，移植规则 |
| 11 | BYOK 安全 | 接受"用户 Key 过云函数" | 域名白名单要求；Key AES 加密+日志禁打印 |
| 12 | NPC 机制 | **两级 NPC**：玩家"创建角色"建的=重要(存npcs/+持久记忆)；未建=次要(LLM即兴,无文件无记忆,可提升) | 开局只建自己，重要角色按需创建 |
| 13 | 剧情 | **无固定剧情走向**，纯沙盒涌现 | 用户明确不要固定剧情；world.md 只做背景 |
| 14 | 运行环境 | 微信开发者工具 + Node.js（**不需要 Java/Python**） | 云函数是 Node.js；caveman 只用规则不跑 Python |
| 15 | **存档** | **多存档系统**：每存档=一次独立游玩，新游戏=建新主角+新存档，存档间完全隔离 | 用户不止玩一次，每次开局自建主角 |
| 16 | **内容尺度** | **允许 18+ 内容**（血腥/暴力/成人/性行为等） | 用户明确要求；与 msgSecCheck 有冲突，见§3.11 |
| 17 | **时间推进** | **世界不会因玩家暂停**：闭关/赶路/养伤/昏迷会推进世界、NPC、线索、传闻 | 解决长期游玩中的冻结感与穿帮 |
| 18 | **重生回复** | **支持 reroll 刷新按钮**：同一轮可重生成回复，但默认不重复推进世界状态 | 提升对话满意度，同时防止刷乱世界线 |
| 19 | **模型配置管理** | **参考 RikkaHub / Chatbox 逻辑**：多配置、本地持久化、Base URL/Key/Model 自定义、一键切换、参数可调 | 用户明确要求自配多套模型与第三方中转 |

---

## 1. 10 + 1 痛点 → 解决方案

| # | 痛点 | 解决方案 | 子系统 |
|---|------|---------|--------|
| 1 | NPC全知 | 实体文件+上下文引擎**代码级**隔离+私有记忆按(存档,NPC)隔离 | 知识库/上下文引擎/记忆 |
| 2 | AI失忆 | **三层记忆**：工作(DB)+情节(NPC记忆文件,持久)+压缩摘要 | 记忆/压缩 |
| 3 | OOC | 角色卡含禁止行为+System约束+可选一致性校验+**重摇按钮** | 角色一致性 |
| 4 | 八股文 | 风格样本+反套路指令+多模型可选+风格注入 | 风格/模型网关 |
| 5 | 剧情松散 | **玩家自驱 + 被动线索追踪**(记玩家自发起的目标,不强推) | 线索追踪/游戏状态 |
| 6 | 不像游戏 | 境界/物品/任务/关系/声望(定性)+状态面板+**自创角色**+**多存档** | 游戏状态/前端/角色创建/存档 |
| 7 | 成本高 | BYOK(开发者0成本)+缓存+压缩+按需加载+导演走主调用 | 模型网关/缓存 |
| 8 | 自由vs结构 | **沙盒优先**：自由输入+世界规则约束+线索追踪可选开 | 场景识别/线索追踪 |
| 9 | 幻觉 | **接地**：事实必来自文件+世界状态文件化+存档持久+记忆只追加 | 知识库/记忆/存档 |
| 10 | 同质化 | **多世界架构**(game_id)+区别化UI+每世界独立+未来UGC | 多世界/前端 |
| 11 | 时间冻结 | **时间推进 + 世界演化**：玩家闭关/赶路时，世界、NPC、线程、传闻同步更新 | 时间引擎/世界演化/NPC演化 |
| 12 | 不是全知却变全盲 | **信息传播机制**：亲见/转述/传闻/公开消息分层传播 | 信息传播/NPC认知 |
| 13 | 回复不满意 | **单轮 reroll**：同一轮可重生成候选回复，锁定前不重复提交状态 | 对话编排/reroll管理 |
| + | 压缩 | caveman-compress 规则移植为压缩 prompt | 压缩 |

---

## 2. 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                 微信小程序前端 (Frontend)                     │
│ 世界选择│存档列表(新游戏/继续/删除)│开局建角│对话│状态面板│     │
│ 模型管理(自配切换,mimo默认)│重摇│压缩│创建角色                │
└──────────────────────────┬──────────────────────────────────┘
                           │ wx.cloud.callFunction (SSE 流式)
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                    云函数层 (Cloud Functions)                │
│                                                              │
│  chat ──┬─► 命令识别(创建角色?) ─是─► 角色创建流程            │
│         │                    ─否─► 上下文引擎(组装,代码级隔离)│
│         ├─► 模型网关(调用户LLM,SSE流式,缓存)                  │
│         ├─► 解析器(REPLY/MEMORY/STATE/DIRECTOR四段)           │
│         ├─► 记忆系统(写工作记忆/情节记忆,仅重要NPC)           │
│         ├─► 线索追踪(被动记玩家目标,不强推)                   │
│         ├─► 游戏状态(应用定性状态变更)                        │
│         └─► 内容安全(msgSecCheck)                            │
│                                                              │
│  createCharacter │ detectScene │ compressContext │ manageModel│
│  manageSave(列表/新建/删除)                                  │
└──┬────────┬────────┬────────┬────────┬────────┬─────────────┘
   ▼        ▼        ▼        ▼        ▼        ▼
┌──────┐┌──────┐┌────────┐┌────────┐┌──────┐
│云存储││云数据库││用户LLM ││微信内容││缓存层│
│共享  ││conv/  ││API BYOK││安全API ││      │
│世界  ││model/ ││流式SSE ││        ││      │
│+存档 ││saves/ ││        ││        ││      │
└──────┘└──────┘└────────┘└────────┘└──────┘
```

---

## 3. 子系统详细设计（15 个）

### 3.1 知识库 (Knowledge Base) — 共享世界 + 每存档隔离
**职责**：.md 文件存世界知识与运行时状态。接地与隔离的物理基础。

**关键：共享 vs 每存档**
- **共享只读**（所有玩家/存档公用）：`world.md`(静态设定)、`regions/`、`items/`、`factions/`
- **每存档隔离**（一次游玩独立）：主角、状态、创建的NPC、NPC记忆、世界动态状态、摘要、线索

```
/games/<game_id>/                       # 共享,只读
  manifest.md                           # 世界清单(标题/简介/初始区域/开局引导语)
  world.md                              # 完整静态世界规则(凡人修仙传人界,详细版~370行)【无剧情】
  world_compressed.md                   # 压缩版世界规则(~90行,约1/6篇幅),每8回合刷新省token
  regions/
    yueguo_qipai.md                     # 越国七派区域
    taiyue_shanmai.md                   # 太岳山脉
    xuese_jindi.md                      # 血色禁地
  items/<item_id>.md                    # 物品(可选)
  factions/<faction_id>.md              # 门派/势力(可选)

/saves/<user_id>/<game_id>/<save_id>/   # 单存档=单次游玩 (完全隔离)
  world_state.md                        # 本存档动态世界状态(谁陨落/宗门兴衰)
  player.md                             # 本存档主角(开局建:姓名/外貌/灵根/门派/境界/背景)
  state.md                              # 本存档定性状态(境界/物品/位置/关系/声望)【无硬数值】
  npcs/<npc_id>.md                      # 本存档创建的重要NPC(玩家用"创建角色"建)
  npc_memory/<npc_id>.md                # 本存档各重要NPC对玩家的私有记忆(只追加)
  summary.md                            # 本存档压缩摘要(caveman规则)
  threads.md                            # 本存档线索追踪(玩家自发起目标)【被动,非主线】
```
> **重要 vs 次要 NPC**：`npcs/` 只有玩家"创建角色"建的重要 NPC。次要 NPC（路边小贩、路人散修）由 LLM 即兴生成，**无文件、无持久记忆**，仅存当前对话上下文；可"创建角色"提升。

**解决痛点**：1 / 9 / 10

#### 3.1.1 重要 NPC 文件格式（玩家创建后生成）
```markdown
---
id: lu_yun
name: 陆云
created_by: player          # 玩家创建=重要
region: yueguo_qipai
faction: san_xiu
realm: lianqi               # 炼气(定性境界)
tags: [剑修]
voice: 沉默寡言
---
# 静态设定
## 外貌 / 性格 / 背景 / 秘密
# 说话风格
- 口头禅：……
- 禁止行为：[…]   # 防OOC
# 风格样本(好/坏)
# 关系
- 玩家：……
# 当前状态(可变)
- mood / location / activity
# 私有记忆区(运行时只追加,仅本NPC)
（玩家当面告知/展示的信息才进这里）
```

### 3.2 上下文引擎 (Context Engine)
**职责**：每次对话前组装**且仅组装**当前场景相关文件。**信息隔离的代码级保证。**
**接口**：`assembleContext({game_id, save_id, user_id, npc_id, region_id, is_important_npc}) → promptSections`
**铁律（代码强制）**：
- 与重要 NPC 陆云对话时，引擎**只查询** `saves/.../save_id/npc_memory/lu_yun.md`，**绝不读**其他 NPC 记忆或其他存档
- 次要 NPC（无文件）：仅注入"次要角色,LLM即兴,不持久"指令，不读记忆
- 防御纵深：代码隔离 + Prompt 指令双保险

#### 3.2.1 场景→文件读取映射
| 场景 | 必读 | 按需读 |
|------|------|--------|
| 与重要NPC对话 | 共享world+当前region + 本存档(world_state+该NPC文件+该NPC私有记忆+player+state+summary+threads) + 最近N轮 | 关联NPC(只读静态公开段) |
| 与次要NPC对话 | 共享world+当前region + 本存档(world_state+player+state+summary) + 最近N轮 | 无记忆文件 |
| 自由探索 | 共享world+当前region + 本存档(state+threads) | region内本存档重要NPC列表 |

#### 3.2.2 世界规则加载策略（token 优化）⚠️ v2.3+ 新增
全量 `world.md`（详细版~370行）很贵。采用**周期刷新**策略：
- **开局 / 压缩上下文后**：注入**完整** `world.md`（全量，此时上下文最空，有空间）
- **每 8 回合**（第8/16/24…轮）：注入**压缩版** `world_compressed.md`（~90行，约1/6篇幅，省token）
- **其余回合**：不注入世界规则（靠上下文中近期对话+NPC文件+region文件维持）
- 用 `turn_counter`（per save）追踪当前回合数；压缩上下文时重置为0

### 3.3 记忆系统 (Memory System)
**职责**：三层记忆，根治失忆。**仅重要 NPC 有情节记忆。**

| 层 | 内容 | 存储 | 适用 |
|----|------|------|------|
| 工作记忆 | 最近 N=20 轮对话 | 云数据库 `conversations`(按save_id) | 所有对话 |
| 情节记忆 | 本重要NPC对玩家的认知(事实) | `npc_memory/<npc>.md` 只追加 | **仅重要NPC** |
| 压缩摘要 | 旧对话按caveman规则压缩成事实 | `summary.md` | 所有对话 |

回合成上下文 = 共享world+region + 本存档(world_state+NPC静态+状态+[重要NPC私有记忆]+摘要+player+state+线索) + 最近20轮 + 风格指令。

### 3.4 模型网关 (LLM Gateway) ⭐核心用户功能
**职责**：BYOK 调用用户自配 LLM，OpenAI 兼容，**流式 SSE**，带缓存，**用户自配+切换**，**mimo 默认预填**。**解决痛点 4、7。**
**接口**：`callLLM({model_config_id, messages, params, stream}) → stream`
- 从 `model_configs` 取配置，AES 解密 key
- POST `{base_url}/chat/completions`（OpenAI 兼容）；**默认预填 base_url=`https://api.xiaomimimo.com/v1`**，用户只需填 key + 模型名
- mimo、各中转站均适用；`stream:true`，云函数 SSE 转发
- **缓存**：相同 (messages哈希, model) 命中（流式不缓存）
- 不打印 key；超时 60s
- **用户自配**：设置页 CRUD 多配置，设默认
- **切换**：对话页可临时切换模型
- **基础参数**：每套配置保存 `temperature` / `max_tokens` / `top_p` / `presence_penalty` / `frequency_penalty` 等常用项
- **本地持久化 + 云端同步**：前端用 `wx.setStorageSync` 持久化当前配置列表与最近选择，云端 `model_configs` 作为跨设备/云函数读取源
- **参考 RikkaHub / Chatbox 的交互逻辑**（经 Tavily 调研）：
  - 配置项以“名称 / API Key / Base URL / 模型ID”为核心
  - 支持多套 provider / model 组合保存与一键切换
  - OpenAI-compatible 模式下可通过 `/v1/models` 获取模型列表；若失败，允许手填模型ID继续使用
  - 保留手动输入优先，不强依赖自动拉取模型列表
  - 常见失败检查聚焦：API Key 错误 / Base URL 错误 / 网络不可达 / 返回模型列表为空

### 3.5 角色一致性 (Character Consistency)
**职责**：防 OOC。**解决痛点 3。**
- 角色卡含"禁止行为"段，注入 System
- **可选一致性校验 pass**：便宜小模型检查回复是否违反角色卡（违规则重生成1次）。默认关
- **重摇按钮 / reroll**：不满意可重新生成，但采用**单轮候选回复机制**：
  - 一次用户输入对应一个 `turn_id`
  - 可生成多个候选 AI 回复（candidate A/B/C…）
  - **只有最终显示并确认保留的那一版**才算正式结果
  - reroll 只能在**下一条用户输入发出前**使用；进入下一轮后锁定
  - 默认 **只重生成 `REPLY` 与表现层文本**，不重复写用户消息、不重复推进时间、不重复追加 NPC 记忆、不重复叠加 world_state / threads / turn_counter
  - 对于闭关/突破/事件结算这类重大状态变更，默认采用“**事实骨架固定，文本表达可重写**”策略，避免刷平行世界

### 3.6 风格系统 (Style System)
**职责**：破八股文。**解决痛点 4。**
- NPC 文件 `voice` 字段 + 风格样本（好/坏示例）
- System 注入反套路指令：禁滥用破折号、禁每段总结、禁高频套话
- 风格注入：`voice: 沉默寡言` → 追加文体指令
- 配合模型网关换模型

### 3.7 线索追踪 (Thread Tracker) — 被动，非主线 ⚠️ redesigned
**职责**：替代原"叙事导演"。**无固定剧情**，只被动记录玩家自发起的目标/未了之事，绝不强推。**解决痛点 5、8。**
- 主 LLM 在 `DIRECTOR` 段产出：识别玩家本回合的"目标/承诺/未了之事"（如"我要找玄铁剑""答应帮张三护法"）→ 写入本存档 `threads.md`
- 当玩家后续遇到相关情境，系统**被动**把相关线索作为提示注入上下文
- **绝不**生成预设剧情走向或强制引导
- **沙盒默认**：线索追踪可关闭（纯沙盒）；开启则只是"提醒你自己定下的目标"

### 3.8 游戏状态 (Game State) — 叙事优先，无硬数值
**职责**：让它"像游戏"不是聊天框，但**无数值束缚**。**解决痛点 6。**
- `state.md`（定性，非数值）：境界(炼气/筑基/结丹/元婴/化神) / 物品(记名不记数) / 位置 / 关系(疏/识/友/亲/仇) / 声望(定性标签) / 目标(见threads)
- LLM 回复的 `STATE` 段提议变更（突破境界/得物/关系变化）→ 解析器校验后应用
- 前端**状态面板**实时显示定性状态（无 HP 条）
- 强化长期反馈：名声变化、关系变化、目标推进、错失机会、新出现机会都应能落到 `state.md` / `threads.md` / `world_state.md` 中被看见

### 3.8A 时间推进 / 世界演化 (Time Progression & World Evolution) ⭐新增
**职责**：解决“玩家不动时世界也暂停”的假感。闭关、赶路、养伤、昏迷、久别重返时，同步推进世界。**解决痛点 11。**
- 新增世界时间轴：`current_date` / `world_time`（例：天南历 412 年三月初七）
- 当发生时间跳跃行为时，系统统一调用时间推进流程：
  1. 推进当前存档日期
  2. 更新玩家状态（修为、伤势、位置、闭关结果）
  3. 更新重要 NPC 动态状态（位置、境界、目标、存活、态度）
  4. 更新区域状态与 `world_state.md`
  5. 更新 `threads.md`（open/resolved/failed/expired/dormant）
  6. 生成“这段时间发生了什么”的汇总给玩家
- 核心原则：**静态设定不改，动态状态按时间推进**
  - 不变：`world.md`、region lore、NPC 核心背景/性格
  - 可变：`world_state.md`、`state.md`、NPC 当前状态、threads、传闻、认知标签
- 建议共享模块：`timeEngine.js` / `worldEvolution.js` / `npcEvolution.js` / `timeSkipReporter.js`

### 3.8B 信息传播 / NPC认知更新 ⭐新增
**职责**：避免从“NPC全知”走到“NPC全盲”，并让 NPC 真正对玩家形成持续看法。**解决痛点 12。**
- 重要 NPC 获取信息的来源分层：
  1. 亲眼所见
  2. 玩家亲口告知
  3. 与其他角色直接交流
  4. 同区域传闻
  5. 门派/坊市公开消息
  6. 高层情报网络
- NPC 记忆不只存事实，还存**对玩家认知标签**，如：无名小辈 / 值得结交 / 心思深 / 可疑 / 不宜招惹 / 可利用
- 这些标签影响：语气、试探强度、报价、是否愿意透露信息、是否轻视玩家
- 长时间跳跃后，还要推进“传闻层”与“最后见闻日期”，避免一年后所有人都还停在旧认知

### 3.9 场景识别 (Scene Detection)
**职责**：A+B 混合 + 命令识别。**解决痛点 8。**
- **命令识别**：输入含"创建角色" → 走角色创建流程（3.14），不当普通对话
- A 方案(显式)：玩家点重要NPC按钮 → 直接路由
- B 方案(隐式)：自由输入 → 轻量分类器判断"在和谁/什么场景交互" → 路由；提及次要NPC则LLM即兴
- MVP 先 A + 命令识别，Phase 2 加 B

### 3.10 压缩 (Compression) — 采纳 caveman-compress 规则
**职责**：保事实丢散文，控 token。**解决痛点 2、7 + 用户需求。**
- 触发：手动按钮 / 超 N 轮自动
- **策略**：旧对话喂 LLM，用 caveman-compress 规则：
  - **必保**：专名(NPC/门派/法宝)、数字/日期、境界、物品名、关系变化、决策、玩家目标、结构
  - **删**：冠词、客套、对冲、连接废话、重复、散文描写
  - 短同义词、片段化、合并冗余
- 产出写入本存档 `summary.md`，旧工作记忆归档
- **接口**：`compress(history, opts) → summary`（云函数 Node.js，调用户 LLM 执行 caveman 规则）
- **注**：移植的是规则为压缩 prompt，不跑原 Python 脚本
- **关键**：保事实不保散文，否则 NPC 忘事实→一致性崩

### 3.11 内容安全 (Content Safety)
**⚠️ 冲突说明**：用户要求允许 18+ 内容（血腥/暴力/成人/性行为），但微信平台要求 `msgSecCheck`。`msgSecCheck` 会拦截大部分成人内容。

**策略（分阶段）**：
- **Phase 1 MVP**：先**跳过 `msgSecCheck`**（或仅对最极端内容做宽松过滤），确保文游核心体验不受限。这有平台违规风险，用户已知悉。
- **Phase 2+**：如果微信审核不过，可改为**自定义宽松过滤**（只拦截真正违法内容如儿童色情/极端暴力，放行正常成人题材）或只在发布公测版时加 msgSecCheck。
- **用户已知悉风险**：微信可能拒审或封禁；个人自用可忽略此风险。

### 3.12 前端 (Frontend)
**职责**：游戏感 UI，区别化设计。**解决痛点 6、10。**
- 页面流：世界选择 → **存档列表(新游戏/继续/删除)** → 开局建角(新游戏时) → 对话页(左定性状态面板/右对话) → 模型管理 → 线索页
- 状态面板：境界/物品/位置/关系/声望/目标（定性，无 HP 条）
- 重要 NPC 列表（本存档的，点击进入对话）+ "创建角色"入口
- 用 `frontend-design` skill 做区别化视觉
- 按钮：发送 / 重摇 / 压缩 / 切换沙盒-线索模式 / 切换模型 / 创建角色

### 3.13 多世界 (Multi-world)
- `game_id` 贯穿所有路径与查询
- `manifest.md` 列世界清单，前端世界选择页
- MVP 只做 1 个世界(凡人修仙传·天南越国)
- 未来 UGC

### 3.14 角色创建系统 (Character Creation) ⭐ 用户新增
**职责**：开局建主角 + 游戏中"创建角色"建重要 NPC。**解决痛点 6。**

**开局（新游戏时）**：
1. 新游戏 → 生成 save_id → 引导玩家创建主角 → `saves/.../save_id/player.md`
2. 两种模式：
   - **快速**：玩家直接描述（"我叫韩立，相貌平平，四灵根，黄枫谷外门弟子"）→ LLM 生成完整 player 卡 → 确认 → 存
   - **访谈**：LLM 逐项问（姓名?外貌?灵根?门派/散修?背景?）→ 生成卡 → 确认 → 存

**游戏中"创建角色"命令**：
1. 玩家在对话框输入"创建角色"（可带描述："创建角色：剑修青年陆云，沉默寡言"）
2. 命令识别拦截（不当普通对话）
3. 两种模式：
   - **快速**：带描述 → LLM 生成完整 NPC 卡(外貌/性格/背景/秘密/说话风格/禁止行为/风格样本) → 玩家确认 → 存 `saves/.../save_id/npcs/<id>.md` → 标记 `created_by: player`
   - **访谈**：不带描述 → LLM 问几轮 → 生成卡 → 确认 → 存
4. 新重要 NPC 出现在本存档 NPC 列表，可对话，有持久私有记忆

**次要→重要提升**：对某即兴次要 NPC 输入"创建角色" → 固化成卡 → 存文件 → 升级为重要（有记忆）

**接口**：`createCharacter({save_id, type:'player'|'npc', description?, interview?}) → character_card`

### 3.15 存档系统 (Save/Load) ⭐ v2.3 新增
**职责**：多存档，每存档=一次独立游玩，完全隔离。**解决痛点 6、9。**

**核心机制**：
- 状态持续落盘：每回合把 npc_memory/state.md/threads.md/world_state.md 写回云存储 → **自动存档**，无需手动存
- "存档"本质 = 选哪个 `save_id` 的文件来读/写
- 存档间完全隔离：不同主角、不同创建NPC、不同世界演变

**存档列表页**（每世界下）：
- 显示所有存档：主角名 + 境界 + 当前位置 + 上次游玩时间
- **新游戏**：生成 save_id → 建主角(3.14) → 进入
- **继续**：选 save_id → 加载其文件 → 进入对话页（恢复到上次状态）
- **删除**：删存档目录 + 相关 DB 记录

**接口**：
- `listSaves({user_id, game_id}) → [save_meta]`
- `createSave({user_id, game_id}) → save_id`（再走建主角）
- `deleteSave({save_id})`
- 加载=对话时所有读写带 `save_id`

**手动存档点（可选,后期）**：危险选择前"标记存档"，可回溯。MVP 先用自动存档。

---

## 4. 数据模型

### 4.1 云数据库 Schema
**`conversations`**（工作记忆,按存档）
```js
{ _id, user_id, game_id, save_id, npc_id, npc_importance:'important'|'minor', role, content, timestamp, scene_type, turn_id, candidate_version, is_final, reroll_count }
// 索引: {user_id:1, game_id:1, save_id:1, npc_id:1, timestamp:1}
```
**`model_configs`**（LLM配置,用户自管,跨存档共享）
```js
{
  _id,
  user_id,
  name,
  base_url,
  api_key_enc,
  model,
  params:{
    temperature,
    max_tokens,
    top_p,
    presence_penalty,
    frequency_penalty
  },
  headers,
  is_default,
  local_persist_key,
  created_at,
  updated_at
}
// 说明: base_url 兼容 OpenAI 风格第三方中转; 前端本地缓存最近使用配置到 wx storage
```
**`saves`**（存档元数据）
```js
{ _id, save_id, user_id, game_id, player_name, player_realm, current_npc, current_region, mode, current_model_config_id, created_at, updated_at, last_played }
// 索引: {user_id:1, game_id:1}
```
**`characters`**（角色元数据索引,便于列表）
```js
{ _id, user_id, game_id, save_id, type:'player'|'npc', npc_id, name, importance, file_path, created_at }
// 索引: {save_id:1, type:1}
```

### 4.2 加密
- `api_key_enc`：云函数环境变量主密钥 AES 加密；日志禁打印 key

---

## 5. 核心数据流

### 5.1 普通对话（与重要 NPC，流式）
```
1. 玩家点"与陆云对话"或自由输入 → 前端调 chat({game_id,save_id,npc_id,user_input,reroll?,turn_id?,model_config_id?})

2. chat 云函数:
   a. 命令识别: user_input 含"创建角色"? → 是 → 转 createCharacter 流程(5.2)
   b. 内容安全: msgSecCheck(user_input) → 命中兜底
   c. 上下文引擎 assembleContext({game_id,save_id,...}):
      - 并行读: 共享(当前region + **world:按策略选完整版/压缩版/不加载**,见3.2.2) + 本存档(world_state+npcs/陆云+npc_memory/陆云+player+state+summary+threads)
      - 查 DB: 本save_id最近20轮 conversations
      - 【代码级隔离】只读本存档陆云的记忆,绝不读他人/他存档
      - 拼 System(world+region+world_state+NPC静态+风格+禁止行为+NPC记忆+摘要+player+state+相关线索+反套路+输出格式) + History + User
   d. 若识别为闭关/赶路/养伤/久别重返等时间跳跃行为 → 先走 `timeEngine` 计算世界推进骨架，再将结果注入本轮上下文
   e. 模型网关 callLLM(stream:true) → SSE 流式(前端打字机显示REPLY)
   f. 流结束,解析 REPLY/MEMORY/STATE/DIRECTOR
   g. 若 `reroll=true`：
      - 复用同一 `turn_id` 与同一事实骨架
      - 仅替换当前候选回复文本
      - 不重复写用户消息/不重复推进时间/不重复追加记忆/不重复叠加 turn_counter
   h. 否则首次提交正式结果：
      - 写库 conversations(user_input + REPLY)
      - 记忆: MEMORY → 追加本存档 npc_memory/陆云.md
      - 状态: STATE → 应用本存档 state.md
      - 线索: DIRECTOR → 更新本存档 threads.md(只记玩家目标,不强推)
      - 若本轮含时间推进 → 落盘 world_state / npc动态状态 / thread状态变化
      - turn_counter +1
   i. (自动存档: 文件已落盘) 返回 {state_updated, objectives, turn_id, rerollable:true}

3. 前端: 刷新状态面板 + 更新目标
```

### 5.2 创建角色命令流
```
1. 玩家输入"创建角色[: 描述]" → 命令识别拦截
2. createCharacter 云函数({save_id, type, description?}):
   a. 带描述 → LLM 一次性生成完整角色卡
   b. 不带 → LLM 启动访谈,逐项追问,收齐后生成卡
3. 玩家确认卡(前端预览可改)
4. 确认后: 写文件 本存档 player.md 或 npcs/<id>.md, 写 characters 索引
5. 返回成功,新角色可用
```

### 5.3 新游戏/读档流
```
新游戏: 世界选择 → 存档列表点"新游戏" → createSave 生成 save_id → 建主角(3.14开局) → 写 player.md → 进入对话页
继续:   存档列表点存档 → 读 save_id 的 state/player → 进入对话页(恢复上次状态)
```

---

## 6. 关键机制深入

### 6.1 信息隔离 5 维度（痛点1根治）
静态设定(每NPC文件) / 世界常识(共享world) / 私有记忆(按**存档+NPC**隔离,代码级只读当前,**仅重要NPC**) / 已知事件(同上,时空关系门控) / 当前状态(NPC文件末段,可变)

### 6.2 三层记忆防失忆（痛点2根治）
工作记忆满 → caveman规则压缩成事实摘要入 summary.md → 旧对话归档。情节记忆(npc_memory)永不压缩(只追加)。重要 NPC 跨会话仍记得你;次要 NPC 不记。**记忆按存档隔离**，A 存档的 NPC 不知 B 存档的事。

### 6.3 沙盒 + 被动线索（痛点5,8平衡）⚠️ 无固定剧情
玩家自驱: 自己建角色、自己定目标。线索追踪器**被动**记录玩家发起的目标,相关情境时提示,**绝不强推预设剧情**。可关(纯沙盒)。区域系统限定作用域。

### 6.4 反幻觉接地（痛点9根治）
铁律: 世界事实必须来自文件,LLM 禁止发明世界设定。世界状态文件化(谁陨落→world_state.md)。存档持久不矛盾重写。NPC 记忆只追加。可选事实校验。

### 6.5 成本控制（痛点7根治）
BYOK(开发者0成本) + 共享world/NPC静态缓存 + caveman压缩 + 按需加载 + 线索追踪走主调用(零额外) + 一致性校验用便宜小模型可选 + 用户可选便宜模型。

### 6.6 叙事无数值（痛点6平衡）
境界=定性tier,物品记名不记数,关系定性,声望标签。状态面板展示定性信息,有游戏感无数值束缚。

### 6.7 两级 NPC（用户新增）
重要(玩家创建,文件+记忆,持久) vs 次要(LLM即兴,无文件无记忆,可提升)。节省成本且符合"重要角色按需创建"。

### 6.8 多存档隔离（v2.3 新增）
共享只读 world + 每存档隔离的动态状态/角色/记忆。自动存档(每回合落盘)。存档间完全独立,可同时维护多个主角的不同人生。

---

## 7. 微信平台约束与应对
| 约束 | 应对 |
|------|------|
| 前端不能调任意URL | LLM 走云函数中转 ✅ |
| 云函数超时(默认3s) | 调60s;流式 SSE |
| 免费额度 | 缓存+按需加载;次要NPC无文件省存储读;共享world缓存 |
| 包大小(主包2MB) | 知识文件在云存储不入包 ✅ |
| 内容安全 | msgSecCheck ✅ |
| 流式支持 | 云函数 SSE / `wx.cloud.callFunction` 流式响应 |

---

## 8. 错误处理
- LLM 超时/不可达 → "模型无响应,请检查配置"
- Key 无效 → "API Key 无效"
- 内容安全命中 → 兜底文案,不写库
- 文件不存在(新存档/次要NPC) → 空记忆/默认 正常进入
- 解析失败(无REPLY) → 兜底;缺MEMORY/STATE → 跳过
- 并发写 npc_memory → MVP 低流量可接受,后期乐观锁
- 流式中断 → 保留已收文本,提示"中断,可重摇"
- 角色创建确认超时/取消 → 不写文件,回对话
- 存档加载失败 → 提示并回存档列表

---

## 9. 测试策略
- **可单测核心逻辑**: 上下文组装(**隔离断言:陆云Prompt不含他人/他存档记忆;次要NPC不读记忆**)、四段解析、定性状态变更、线索提取、"创建角色"命令识别、角色卡生成格式、存档隔离(两存档数据不互窜)
- **Mock LLM**: 固定 canned 响应
- **端到端**: 真实 mimo API 手动跑: 新游戏建角→对话→记忆隔离→创建NPC→线索追踪→流式→多存档切换

---

## 10. 实现阶段（4 phase,每阶段可玩可测）

| Phase | 子系统 | 产出 | 验证痛点 |
|-------|--------|------|---------|
| **0 环境** | — | 装微信开发者工具+Node.js,注册AppID,开通云开发 | — |
| **1 MVP** | 知识库(凡人修仙传越国,无剧情)+上下文引擎+三层记忆+模型网关(自配+切换+流式,**mimo默认**)+**角色创建(开局建角+创建角色命令+两级NPC)**+**存档系统(多存档/新游戏/继续/删除/自动存档)**+角色一致性(基础)+前端(基础+状态面板+存档列表)+内容安全 | 可玩demo: 新游戏建角→对话→记忆隔离→跨会话不失忆→创建重要NPC→多存档切换→自管模型→流式 | 1,2,3,4,6,7,9 |
| **2 体验** | 压缩(caveman规则)+场景识别(B)+风格系统 | 自由输入+长对话不崩+破八股+次要NPC即兴 | 2,4,8 |
| **3 游戏** | 游戏状态(定性)+线索追踪(被动) | 有境界/物品/关系/目标追踪,像游戏 | 5,6,8 |
| **4 多样** | 多世界+角色一致性(重摇/严格)+前端精修 | 多世界切换+稳健+区别化UI | 3,10 |

> 角色创建 + 两级NPC + 多存档 + 流式 + 模型自配切换 都在 Phase 1（用户明确要的核心）

---

## 11. 假设与开放问题

**已确认（v2.3）**: 范围可扩展优先 / demo凡人修仙传越国无剧情 / 多用户小规模 / OpenAI兼容BYOK+自配切换+mimo默认(`https://api.xiaomimimo.com/v1`) / MVP流式 / 叙事无硬数值 / msgSecCheck / 四段输出 / caveman压缩规则 / 接受BYOK过云函数 / 两级NPC+创建角色命令 / 无固定剧情沙盒 / 环境=微信开发者工具+Node.js(无需Java/Python) / **多存档系统**

**已确认补充（v2.3 末轮）**:
- mimo 模型名 = `mimo-v2.5-pro`
- 玩家主角无固定默认：每次新游戏都走建角流程，每次主角不同
- "创建角色"触发词 = 匹配"创建角色"4字开头（可带冒号+描述）
- 存档上限 = 每用户每世界 10 个

**剩余开放（不阻塞,实现时再定）**: 无。所有用户决策项已确认。

---

## 12. 风险与未决
- **格式遵从性**: LLM 可能不按四段输出 → 容错解析+Prompt 调优
- **流式+四段解析**: 流式只推 REPLY,其余三段流末尾一次性处理
- **次要NPC一致性**: 即兴角色可能前后不一致 → 提示LLM保持当前场景内一致;提升为重要后才有持久一致性
- **角色卡生成质量**: LLM 生成的 NPC 卡可能不合预期 → 玩家确认环节可改
- **线索追踪≠主线**: 严格保证只被动记录,不演变成强推剧情 → Prompt 严格约束
- **存档隔离**: 必须确保 save_id 贯穿所有读写,严防串档 → 代码级校验+测试
- **记忆膨胀**: npc_memory 增长 → Phase2 与压缩一并设计遗忘策略
- **内容安全误杀**: 兜底+后期申诉
- **免费额度耗尽**: 监控+缓存
- **caveman压缩丢事实**: 压缩规则强调保事实+校验

---

## 13. 下一步
本 spec 经你确认后 → `writing-plans` skill 写 **Phase 0（环境）+ Phase 1（MVP）** 逐步实现计划(2-5分钟一步)。Phase 2-4 各自再走 spec→plan→implement 循环。
